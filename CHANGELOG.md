# 变更记录

## 2026-09-22

### 新增

- 新增 Android 内测版账号密码注册与登录、内测口令保护、App 内账号注销，以及用户协议和隐私政策页面。
- 新增原生隐私弹窗、Android 包名/权限/ABI 配置、系统相册附件、系统分享与外部链接打开能力，并提供 DCloud AppID 构建配置脚本。
- 新增 App Push 设备登记、提醒投递去重与可配置 HTTPS 投递桥；未开通 UniPush 时保持站内提醒并明确显示未配置状态。
- 固定 DCloud AppID、Android 包名和内测版本，生成并接入发布签名、全套应用图标与通知栏小图标。
- 启用 UniPush 2.0 原生离线 SDK，新增带 Bearer Token 鉴权的 uniCloud HTTPS 投递桥、扩展库配置和依赖数据表。

### 修复

- App 邀请、新闻原文、数据源和公开内容分享改用对应平台能力，不再把 Android 一律按小程序复制链接处理。
- App 选择附件明确限制为图片，退出登录时同步注销 Push 设备，删除账号时清理会话、附件、共享关系和提醒数据。
- 隐私政策明确披露 DCloud、个推、厂商推送通道处理的信息及完全关闭 App 后通知所需条件。

### 验证

- 39 项服务端测试、TypeScript 类型检查以及 H5、微信小程序、App 资源构建均通过。
- 用户协议和隐私政策在 320、390、430 像素视口下无横向溢出或过小交互控件，并完成截图检查。

## 2026-09-17

### 新增

- 新增共享果冻 Tab 与完成状态图标组件，并在首页筛选、搜索方式、比赛和新闻页面统一使用。
- 新增根目录 `AGENTS.md`，把已确认的编辑态、图标居中、搜索结果、Tab 动效和移动端验收规则固化为后续 AI 开发约束。

### 修复

- 子页面顶部返回栏改为吸顶安全区，页面滚动后内容不再进入微信状态栏和右上角胶囊区域；新闻卡片移除与顶部来源信息重复且会被真机挤压的底部标签。
- 小空间昵称默认只读展示，点击编辑后才出现输入框、保存与取消；重绘并居中双人爱心。
- 比赛“已设置”状态保留清晰的铃铛图标；提醒完成对号按圆心居中；新闻卡片标签在窄屏下保持垂直居中且不溢出。
- 本地及联网搜索有可点击来源时只展示紧凑的逐行结果，不再重复输出一段原始摘要。

## 2026-09-16

### 新增

- 提醒编辑页新增可选的微信“服务通知”授权；接入日历提醒订阅模板（提醒事项、提醒时间、备注），到点后与站内提醒并行发送，微信失败不会丢失站内通知。
- 服务端新增订阅授权记录、到期投递状态和结构化失败日志；共享提醒仅向本人实际授权的订阅发送，重复提醒明确按下一次提醒单次授权。
- “小空间”从首页内嵌视图拆为无底部导航的独立页面，通过原生页面栈支持顶部返回与微信边缘右滑返回。
- “搜搜”新增“智能、只搜本地、问模型”三种明确策略；可以在本地待办之外直接询问公开问题，也可以在本地结果后继续问模型。
- 智能搜索新增兼容 Chat Completions 模式，并默认预置微信 Coding Plan 网关与 `Deepseek-v4-flash`；API Key 保持服务端空配置，由部署者填写。
- “问问”升级为“搜搜”真实智能搜索：先检索当前用户可见的小记、提醒、比赛和新闻，本地不足时再调用已配置模型服务的联网搜索；回答展示可点击引用，不使用模拟答案。
- “小记”前新增昵称头像并作为“我们”入口，底部导航收敛为“随记、提醒、搜搜”三项，窄屏下保持完整标签与 44px 点击区域。
- 接入 football-data.org 的英超、西甲、欧冠真实赛程、赛果与积分榜，以及 PandaScore 的英雄联盟赛程和赛事排名。
- 足球赛程、详情和积分榜统一展示中文球队名，并补充英超、西甲及常见欧冠参赛队映射；比赛阶段同步改为中文。
- 英雄联盟只同步 LPL 与全球总决赛：服务端动态解析 PandaScore 联赛 ID、在上游请求中限定联赛，并对响应及旧缓存再次过滤。
- 生产新闻源改为无需密钥且允许商业使用的 GDELT Project：默认每小时以单个聚合查询拉取真实中文财经、商业和科技资讯，再按真实标题整理精选、股市和热点频道；保留 NewsAPI 作为显式可选兼容源。
- GDELT 在服务器网络不可达或返回异常数据时，自动改读 36氪与中新网财经公开 RSS；保存真实标题、发布时间、来源、原文链接及 RSS 实际提供的摘要，不抓取网页正文或生成内容。
- 新增 GDELT 署名及来源链接、紧凑时间解析、来源去重和 429 保留旧缓存；H5 可直接打开数据源页面，小程序与 App 可复制来源链接。数据源未提供摘要或正文时明确留空，不抓取或补造内容。
- 新增服务端定时同步、手动刷新限流、SQLite 最后成功数据缓存和结构化 provider 错误码。
- 新增服务端图片缓存：队徽和新闻配图经 HTTPS、公网地址、大小、类型及文件特征校验后，按 SHA-256 保存到持久化目录并由同域接口提供。
- 新增 AI 服务状态、权限隔离、每用户限流、超时与安全错误映射；本地资料和网页查询严格拆成两次请求，私人笔记不会进入网页搜索词。
- 新增图片缓存容量上限与引用清理，以及 30 天/1000 条的新闻摘要历史，避免磁盘无限增长并保障旧收藏可读。

### 修复

- “问模型”会跳过本地命中，且联网请求仍只发送当前问题；“只搜本地”不会误触发模型调用。
- 搜搜在模型已返回正文但未提供网页引用时，不再丢弃整段回答；改为展示“模型回答 · 未联网核验”及醒目说明，有真实引用时仍保持联网回答样式。
- 重绘搜搜页的隐私锁图标，补齐锁梁、锁体和锁孔的视觉关系；联网状态改为“已配置/异常”，不再仅凭存在 token 就误报“联网可用”。
- 搜搜不再因模型未配置而禁用发送箭头：登录后可始终执行服务端本地检索，本地无匹配时才尝试联网；联网未配置、不可达或缺少真实引用时统一显示“没有找到相关数据”。
- 本地搜索改为确定性权限内检索，不再把私人小记片段发送给模型；联网阶段仍只发送当前问题，并在服务端记录不可用原因便于排查。
- Chat Completions 模式会请求 `/chat/completions`、解析 `choices[0].message.content` 与 `search_results`，不再把兼容服务错误地当作 Responses API；无可核验搜索来源时转换为无数据状态。
- 修复 GDELT 在国内服务器连接失败后新闻页完全无数据的问题，同时保留可追踪的实际 provider 标记。
- 赛事分类固定为英超、欧冠、英雄联盟、西甲；切换赛事后自动回到近期赛程，赛程/赛果/积分榜使用弹性滑动胶囊反馈。
- PandaScore 未提供某个淘汰赛阶段积分榜时保留真实赛程和赛果，并显示友好说明，不再暴露上游 404 JSON。
- 新闻详情展示 RSS 数据源实际提供的摘要，并避免摘要在页面中重复显示。
- 重做比赛与新闻页按钮的对齐、尺寸、禁用/按压状态和小屏滚动定位，避免选中赛事被截断。
- 比赛、新闻及详情页移除所有本地模拟数据；无缓存时直接显示可排查的配置或上游错误，有缓存时保留最后一次真实数据并标记过期告警。
- 比赛提醒改为创建真实站内提醒；新闻主题标签改为可用筛选，文章卡片补充按压反馈与可访问语义。
- 比赛提醒使用稳定来源标识幂等写入，同一用户重复设置同一场比赛会更新原提醒，不再产生重复记录。
- 备忘详情编辑/删除按钮恢复等宽，底部导航按左右安全区正确居中。
- 新闻原文只允许 HTTP(S) 地址，同一频道按稳定 ID 去重；请求增加 12 秒超时并避免慢响应覆盖新选择。
- 修复新闻历史归档把 provider 固定写成 NewsAPI 的问题，收藏详情现在会保留实际聚合来源；GDELT 出版方图片因授权不明默认不缓存或重托管。

### 验证

- 新增中文队名、LPL/全球总决赛限定、RSS 实时兜底、真实 provider 转换、无配置错误、过期缓存回退、刷新限流、图片读取及 SSRF 防护测试。
- 新增智能搜索的本地/联网分支、相对日期提醒、引用解析、伪造来源过滤及私人/共享内容权限隔离测试。
- TypeScript、服务端测试、H5、微信小程序和 App 构建，以及 320/375/430 像素移动端视觉回归均通过。

## 2026-09-15

### 新增

- 新增比赛首版页面，支持英超、西甲、欧冠、英雄联盟分类，以及近期赛程、历史赛果、积分榜和比赛详情。
- 新增新闻首版页面，支持精选、股市、热点分类，以及文章详情和本地收藏状态。
- 新增独立的备忘详情、编辑、邀请绑定和提醒消息页面，统一使用原生页面栈。
- 新增移动端截图与布局审计脚本，覆盖 320、375、390、430 像素常见屏宽。

### 修复

- 修复列表时间直接显示 GMT 原始字符串的问题，统一为跨端中文日期时间格式。
- 修复“我们俩”和“置顶”信息挤在一起、时间与标签错位的问题。
- 修复详情页返回按钮位置异常，以及子页面未入栈导致左滑直接退出小程序的问题。
- 统一返回、提醒、定位、链接、附件、刷新和收藏图标的尺寸、描边与点击区域。
- 修复窄屏下赛事分类、球队名称、积分榜和资讯标签的换行与溢出问题。
- 修复比赛详情近期战绩样式在微信上传编译器中的兼容性问题。

### 说明

- 比赛与新闻当前为明确标注的设计示例，使用固定示例时间，不代表实时资讯、比分或投资建议。
- 示例赛事暂不创建真实开赛提醒；接入合规实时数据源后再启用。
- 微信小程序顶部返回按钮已使用真实页面栈；iOS 微信边缘返回手势仍需在正式体验版真机验收。

### 验证

- TypeScript 类型检查、服务端自动化测试、H5、微信小程序和 App 资源构建均已通过。
- 320、375、390、430 像素移动端页面审计未发现页面级横向溢出或过小交互控件。
